all:
  children:
    private_servers:
      hosts:
        si-s-serv1751.chuliege.be:
      vars:
        ansible_host: si-s-serv1751.chuliege.be
        ansible_user: setmeld
        ansible_password: RG1XU72/nFs/'p"7DZs

        # --- OVH The Bastion Wrapper Variables ---
        bastion_user: f093778
        bastion_host: bastion-master.segi.uliege.priv
        bastion_port: 22

        # Ensure your local SSH key is used for the ingress connection to the bastion
        ansible_ssh_private_key_file: ~/.ssh/id_rsa

        # --- Deployment ---
        base_url: si-s-serv1751.chuliege.be
        nginx_port: 80
        ssl_enabled: true
        ssl_email: jackson@setmeld.com

import { LdoJsonldContext, LdSet } from "@ldo/ldo";

/**
 * =============================================================================
 * Typescript Typings for statistics_mean_query
 * =============================================================================
 */

/**
 * MeanQuery Type
 */
export interface MeanQuery {
  "@id"?: string;
  "@context"?: LdoJsonldContext;
  queryVersion?: string;
}
